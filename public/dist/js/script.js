window.addEventListener('load', function (e) {
    if ($("#cardTo_table").DataTable().length > 0) {
        $("#cardTo_table").DataTable()
    }
    if ($("#sliderTop_table").DataTable().length > 0) {
        $("#sliderTop_table").DataTable()
    }
});